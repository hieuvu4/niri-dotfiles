# my-niri-dotfiles

```
sudo pacman -S git gnome-keyring  pipewire-pulse pipewire-alsa pipewire-jack  nwg-look v4l2loopback-dkms jq dbus
```





# Essentials

```
sudo pacman -Syu niri xorg-xwayland xwayland-satellite xdg-desktop-portal-gnome xdg-desktop-portal-gtk waybar swww pipewire wireplumber pulsemixer
```

```
sudo pacman -S kitty htop unzip vim neovim ranger  python-pillow python-pywal fastfetch
```


## Fonts
```
sudo pacman -S noto-fonts ttf-jetbrains-mono-nerd
```

## Pywal for Firefox and Spotify-Launcher 

```
sudo pacman -S firefox spotify-launcher
```


```
yay -S python-pywalfox yay -S spicetify-cli
```
```
yay -S spicetify-cli
```

```
curl -fsSL https://raw.githubusercontent.com/spicetify/marketplace/main/resources/install.sh | sh
```

```
spicetify backup apply
```

```
spicetify update
```


# Gaming

```
sudo pacman -S steam 
```

```
sudo pacman -S mesa lib32-mesa vulkan-radeon lib32-vulkan-radeon libva-mesa-driver libva-utils
```

## Discord (Vesktop)

```
ln -s $HOME/.cache/wal/vesktop.css $HOME/.config/vesktop/themes/pywal-vesktop.theme.css
```

```
ls -l ~/.config/vesktop/themes/pywal.theme.css
```